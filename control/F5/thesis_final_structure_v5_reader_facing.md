# thesis_final_structure_v5_reader_facing.md

## 最终推荐结构

本结构服从导师格式要求，同时避免内部项目治理痕迹。正文面向读者，不面向项目文件。

---

# Введение

应包含：
- 研究背景；
- 文献综述，引用格式为 [1], [2]；
- 研究问题；
- 本文内容按章节概述。

禁止出现：F1/F2/F3/F4、Main、d08、run_id、clean-lineage。

---

# Постановка задачи

应包含：
- 市场中有 n 个风险资产；
- 收益向量、协方差矩阵、组合权重；
- long-only 可行域；
- CtR、CtB、D_R、D_B；
- CtR–CtB 模型；
- 需要通过程序求解和比较的策略；
- 输出结果的类型：表格、图、数值诊断、程序清单。

---

# 1. Классические модели портфеля: модели Марковица, Тобина и Шарпа

## 1.1 Доходность, риск и ковариационная матрица

来自笔记1、笔记3和旧课程论文。

## 1.2 Модель Марковица

保留均值—方差优化、有效边界和 GMV 背景。

## 1.3 Модель Тобина

保留无风险资产、风险资产组合、资本市场线和分离思想。

## 1.4 Модель Шарпа

保留单指数模型、市场指数、beta、特质风险和协方差简化。

## 1.5 Переход к CtR и CtB

说明经典模型提供风险—收益背景，但本文转向风险贡献和相关结构。

---

# 2. Вклад в риск CtR и риск-бюджетирование

应包含：
- 组合波动率；
- MRC；
- CtR；
- Euler 分解；
- 标准化 CtR share；
- 风险预算；
- D_R；
- CtR-only 基准。

---

# 3. Корреляция с портфелем CtB

应包含：
- CtB 定义；
- D_B；
- CtB-only 基准；
- CtR 与 CtB 的联系；
- 为什么 CtB 是结构约束对象。

---

# 4. Согласованная модель CtR–CtB

应包含：
- CtR–CtB 硬约束模型；
- soft penalty 实现；
- 参数解释；
- 为什么不是对称双目标；
- 退化到 CtR-only 的情况。

正文统一使用“модель CtR–CtB”，不使用 Main。

---

# 5. Численный метод и программная реализация

应包含：
- 六类策略；
- PG → damped Newton；
- simplex projection；
- fallback；
- diagnostics；
- GMV 单独求解；
- 程序 listing 放附录。

---

# 6. Данные, базовые стратегии и калибровка

应包含：
- ETF universe；
- 样本切分；
- 协方差估计；
- baseline 集合；
- 主规格选择；
- 更严格 CtB 约束规格作为 robustness。

禁止正文出现 d08/d04。

---

# 7. Результаты расчетов и интерпретация

应包含：
- 结构结果；
- 动态结果；
- 资本配置 vs 风险贡献；
- 绩效结果；
- 数值可靠性；
- 统计推断边界。

结论必须克制：CtR–CtB 是结构折中模型，不是收益冠军。

---

# Заключение

按点列出完成内容：
1. 研究了 Markowitz、Tobin、Sharpe；
2. 定义了 CtR；
3. 定义了 CtB；
4. 构造了 CtR–CtB 模型；
5. 实现了数值方法；
6. 完成了 ETF 实验；
7. 完成了参数校准；
8. 完成了 robustness；
9. 完成了程序 listing。

---

# Список литературы

至少 10 项，编号引用。

---

# Приложение A. Дополнительные таблицы и статистический вывод

包含更严格 CtB 规格、统计推断摘要、solver fallback 表。

---

# Приложение B. Минимальная схема воспроизведения

包含最小复现流程，不放过多项目治理语言。

---

# Приложение C. Листинги программ

包含 Portfolio05 主线程序 listing。

可加说明：
> В программной реализации стратегия CtR–CtB имеет техническую метку Main.
